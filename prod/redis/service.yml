apiVersion: v1
kind: Service
metadata:
  name: redis                # this is REDIS_HOST — must match configmap exactly
  namespace: inventory-prod
spec:
  clusterIP: None            # headless — stable DNS for StatefulSet
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379